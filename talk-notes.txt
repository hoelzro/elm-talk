Elm Talk

  - I'm a backend dev; major props to FE devs, because I really struggle with the state of things
  - I like FP
  - Elm made me care about frontend again
  - Elm is like if Haskell and ML had a baby and it was raised by wild JS
  - Elm is FRP
  - I think Elm is a nice gateway to languages like Haskell
  - Type annotations are optional, but inferred

Signals
Tasks
Debugger
elm-check
elm-examples
